import { BehaviorsDirective } from './behaviors.directive';

describe('BehaviorsDirective', () => {
  it('should create an instance', () => {
    const directive = new BehaviorsDirective();
    expect(directive).toBeTruthy();
  });
});
