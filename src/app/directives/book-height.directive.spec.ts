import { BookHeightDirective } from './book-height.directive';

describe('BookHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new BookHeightDirective();
    expect(directive).toBeTruthy();
  });
});
